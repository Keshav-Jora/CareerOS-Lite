import type { AdminActivity, AdminUserSummary } from './analyticsMetrics';

export function RecentActivityTable({ activities, users }: { activities: AdminActivity[]; users: AdminUserSummary[] }) {
  const byId = new Map(users.map((user) => [user.uid, user]));
  return <section className="rounded-2xl border border-slate-800 bg-slate-900/70 shadow-sm"><div className="border-b border-slate-800 p-5"><h2 className="text-base font-semibold text-slate-100">Recent activity</h2><p className="mt-1 text-sm text-slate-400">Latest product actions across your users.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="border-b border-slate-800 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3 font-medium">User</th><th className="px-4 py-3 font-medium">Action</th><th className="px-4 py-3 font-medium">Page</th><th className="px-4 py-3 font-medium">Time</th></tr></thead><tbody>{activities.map((activity) => { const user = byId.get(activity.userId); return <tr key={activity.id} className="border-b border-slate-800/80"><td className="px-5 py-3 text-slate-200">{user?.displayName ?? user?.email ?? activity.userId}</td><td className="px-4 py-3 text-slate-300">{activity.action}</td><td className="px-4 py-3 text-slate-500">{activity.page}</td><td className="px-4 py-3 text-slate-500">{activity.timestamp.toLocaleString()}</td></tr>; })}</tbody></table></div>{activities.length === 0 && <p className="p-6 text-center text-sm text-slate-500">No activity has been recorded yet.</p>}</section>;
}
